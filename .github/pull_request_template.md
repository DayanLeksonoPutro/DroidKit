## What does this PR do?

Brief description of the change.

## CLI command wrapped (if new feature)

```bash
# example: adb shell dumpsys meminfo
```

## Checklist

- [ ] Logic added to the correct module (`src/android/` or `src/avd/`)
- [ ] Command registered in `extension.ts`
- [ ] `package.json` updated (command + menu if needed)
- [ ] `README.md` updated
- [ ] `CHANGELOG.md` updated under `[Unreleased]`
- [ ] No new npm runtime dependencies added
